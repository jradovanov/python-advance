Chat GTP

Разбирам. С удоволствие ще ви помогна с задачата във Python, свързана с *args и **kwargs.

В Python, *args и **kwargs са специални параметри, които могат да се използват при дефиниране на функции
за приемане на произволен брой аргументи.

*args се използва, когато искаме да подадем произволен брой позиционни аргументи на функцията.
args е кортеж (tuple), който съдържа всички подадени позиционни аргументи. Например:

python
Copy code
def my_function(*args):
    for arg in args:
        print(arg)

my_function(1, 2, 3)
Резултатът от изпълнението на горния код ще бъде:

Copy code
1
2
3
**kwargs се използва, когато искаме да подадем произволен брой именовани аргументи на функцията.
kwargs е речник (dictionary), който съдържа всички подадени именовани аргументи. Например:

python
Copy code
def my_function(**kwargs):
    for key, value in kwargs.items():
        print(key, value)

my_function(a=1, b=2, c=3)
Резултатът от изпълнението на горния код ще бъде:

css
Copy code
a 1
b 2
c 3
Във вашата задача, може да се използват *args и **kwargs за да се приемат произволен брой аргументи и
ключ-стойностни двойки съответно.
